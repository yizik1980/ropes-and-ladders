{
  "name": "@ropes-and-ladders/client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node --env-file-if-exists=.env build.js",
    "dev": "node --env-file-if-exists=.env build.js --watch"
  },
  "devDependencies": {
    "cssnano": "^6.0.0",
    "esbuild": "^0.19.0",
    "html-minifier": "^4.0.0",
    "postcss": "^8.4.0"
  },
  "dependencies": {
    "socket.io-client": "^4.8.0"
  }
}
